from setuptools import find_packages
from setuptools import setup

setup(
    name='yiwen.date',
    version='0.1.0',
    license='MIT',
    packages=find_packages(),
    install_requires=[
        'ipython',
    ],
)
